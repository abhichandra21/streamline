#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
set -a; source "$SCRIPT_DIR/.env"; set +a
source "$SCRIPT_DIR/venv/bin/activate"

case "${1:-}" in
    setup)
        shift
        exec python3 -m recommender.setup "$@"
        ;;
    *)
        exec python3 -m recommender.main "$@"
        ;;
esac
